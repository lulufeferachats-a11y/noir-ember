# Which restaurant tenant this build serves (must match a `slug` in the restaurants table)
VITE_RESTAURANT_SLUG=noir-and-ember

# Base URL for the API. Leave as /api in dev (proxied by Vite to the server).
VITE_API_BASE_URL=/api
